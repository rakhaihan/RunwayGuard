# Model Weights

Place YOLO `.pt` or ONNX exports here. Large binaries must not be committed.

| File | Purpose |
|------|---------|
| `yolo/best.pt` | Trained YOLO weights (future) |
| `yolo/best.onnx` | CPU-optimized export (future) |
